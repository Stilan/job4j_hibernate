create table j_markAuto (
    id serial primary key,
    name varchar(2000)
);

create table j_modelAuto (
    id serial primary key,
    name varchar(2000)
);

create table book (
    id serial primary key,
    name varchar(2000)
);

create table author (
    id serial primary key,
    name varchar(2000)
);
select * from author;
